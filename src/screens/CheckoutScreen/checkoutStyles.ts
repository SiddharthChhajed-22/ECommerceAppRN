import { StyleSheet } from 'react-native';

export const checkoutStyles = StyleSheet.create({
  // Screen-specific styles if needed
  // Template styles are in ui/templates/CheckoutTemplate/styles.ts
});
