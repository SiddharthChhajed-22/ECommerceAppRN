import { StyleSheet } from 'react-native';

export const profileStyles = StyleSheet.create({
  // Screen-specific styles if needed
  // Template styles are in ui/templates/ProfileTemplate/styles.ts
});
