import { StyleSheet } from 'react-native';

export const ordersStyles = StyleSheet.create({
  // Screen-specific styles if needed
  // Template styles are in ui/templates/OrderListTemplate/styles.ts
});
