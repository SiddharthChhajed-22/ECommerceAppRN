import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  // Screen-specific styles if needed
  // Template styles are in components/templates/HomeTemplate/styles.ts
});
