export const APP_NAME = 'EcommerceAppTS';

export const HARD_CODED = {
  defaultPageSize: 10,
  productCurrency: '₹',
  authTokenKey: '@ecomapp/token',
};

export const STATIC_DELAY_MS = 500;

export const SECURITY = {
  useHttpsOnly: true,
};

